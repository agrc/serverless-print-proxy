{
  "name": "serverless-print-proxy",
  "version": "2.0.12",
  "description": "A web service for switching out quad words in print requests to ArcGIS Server.",
  "repository": "https://github.com/agrc/serverless-print-proxy",
  "license": "MIT",
  "author": "Scott Davis <stdavis@utah.gov>",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "check": "tsc --noEmit",
    "format": "prettier . --write",
    "lint": "eslint *.ts",
    "start": "concurrently \"pnpm run build:watch\" \"pnpm run start:firestore\" \"FIRESTORE_EMULATOR_HOST=127.0.0.1:8081 NODE_ENV=development nodemon dist/index.js\"",
    "start:debug": "concurrently \"pnpm run build:watch\" \"pnpm run start:firestore\" \"FIRESTORE_EMULATOR_HOST=127.0.0.1:8081 NODE_ENV=development DEBUG=http-proxy-middleware* nodemon dist/index.js\"",
    "start:firestore": "./scripts/start-emulators.sh",
    "start:prod": "NODE_ENV=production node dist/index.js",
    "test": "vitest",
    "test:ci": "pnpm exec firebase emulators:exec --only firestore 'node scripts/seed-test-data.ts && vitest'"
  },
  "dependencies": {
    "@google-cloud/firestore": "^8.6.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.2.0",
    "http-proxy-middleware": "^4.1.0"
  },
  "devDependencies": {
    "@total-typescript/tsconfig": "^1.0.4",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "@types/supertest": "^7.2.0",
    "@ugrc/eslint-config": "^1.3.0",
    "concurrently": "^10.0.3",
    "eslint": "^10.3.0",
    "firebase-tools": "^15.19.1",
    "nodemon": "^3.1.14",
    "prettier": "^3.8.3",
    "prettier-plugin-organize-imports": "^4.3.0",
    "prettier-plugin-packagejson": "^3.0.2",
    "supertest": "^7.2.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8"
  }
}
